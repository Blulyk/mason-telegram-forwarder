name: Build Docker image

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push Telegram Forwarder
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ghcr.io/blulyk/telegram-forwarder:0.3.0
            ghcr.io/blulyk/telegram-forwarder:latest

      - name: Build and push WhatsApp Bridge
        uses: docker/build-push-action@v7
        with:
          context: ./whatsapp-bridge
          push: true
          tags: |
            ghcr.io/blulyk/whatsapp-bridge:0.1.0
            ghcr.io/blulyk/whatsapp-bridge:0.1.1
            ghcr.io/blulyk/whatsapp-bridge:0.1.2
            ghcr.io/blulyk/whatsapp-bridge:latest
